# ── Phase 1–3 (unchanged) ────────────────────────────────────────────────────
pypdf>=4.0.0
pdfplumber>=0.11.0
regex>=2024.0.0
chromadb>=0.4.22
sentence-transformers>=2.2.0
protobuf>=3.20,<4
jupyter>=1.0.0
ipykernel>=6.0.0
pandas>=2.0.0
pyyaml>=6.0.0
openai>=1.0.0
pytest>=7.0.0
python-dotenv>=1.0.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# ── Phase 4: Auth + Multi-tenancy ────────────────────────────────────────────
# Auth
python-jose[cryptography]>=3.3.0   # JWT RS256
passlib[bcrypt]>=1.7.4             # password hashing

# Async DB
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0                  # SQLite async driver (dev)
asyncpg>=0.29.0                    # PostgreSQL async driver (prod)

# Rate limiting
slowapi>=0.1.9

# Migrations (run alembic init / alembic revision / alembic upgrade)
alembic>=1.13.0

# Email validation
pydantic[email]>=2.0.0

# ── 2FA ───────────────────────────────────────────────────────────────────────
pyotp>=2.9.0          # TOTP generation + verification
qrcode[pil]>=7.4.2    # QR code for Google Authenticator setup
