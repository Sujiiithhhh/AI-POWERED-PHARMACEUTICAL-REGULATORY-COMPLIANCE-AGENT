# PharmaCheck — Combined Requirements
# Install with: pip install -r requirements_full.txt

# ── Core compliance engine (Phase 2) ─────────────────────────────────────────
pyyaml>=6.0
chromadb>=0.4.22
openai>=1.0.0
python-dotenv>=1.0.0
pypdf>=3.0.0

# ── SaaS backend (Phase 4) ───────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0          # SQLite for dev; swap for asyncpg in prod
asyncpg>=0.29.0            # PostgreSQL async driver (prod)
python-jose[cryptography]>=3.3.0   # JWT RS256
passlib[bcrypt]>=1.7.4     # Password hashing
python-multipart>=0.0.9    # File upload support
slowapi>=0.1.9             # Rate limiting
pydantic>=2.0.0
httpx>=0.27.0              # Async HTTP client

# ── 2FA ───────────────────────────────────────────────────────────────────────
pyotp>=2.9.0
qrcode[pil]>=7.4.2
